{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "mermaid-renderer",
  "description": "Event-only capability for the hidden Mermaid rendering WebView",
  "windows": ["mermaid-renderer"],
  "permissions": ["core:event:allow-listen", "core:event:allow-emit-to"]
}
