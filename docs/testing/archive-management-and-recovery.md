# Archive management and recovery verification

Server coverage verifies authenticated bounded listing, authorization-scoped artifact lookup, 60-second forced attachment URLs, opaque not-found responses, role-gated invalidation, export/evidence invalidation in one transaction, and metadata-only audit.

Desktop coverage verifies complete local public-reader validation before document replacement, strict lower Desktop budgets, Canonical identity preservation, monotonic local revision lookup, and no returned document for corrupt input.

Future Asset materialization tests must prove atomic adoption, content-addressed deduplication, Asset-ID mapping, rollback, quota enforcement, inspection-policy compatibility, and no partial document replacement.
